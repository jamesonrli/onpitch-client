var Profile = function(firstName, lastName, imageURL, headline) {
  this.firstName = firstName;
  this.lastName = lastName;
  this.imageURL = imageURL;
  this.headline = headline;
};

module.exports = Profile;
